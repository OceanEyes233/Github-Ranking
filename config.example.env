# GitHub API Token (可选，但建议配置以获得更高的 API 速率限制)
# 在 https://github.com/settings/tokens 创建一个 Personal Access Token
GITHUB_TOKEN=

# Notion API Key (必需，如果要保存到 Notion)
# 在 https://www.notion.so/my-integrations 创建一个 Integration
NOTION_API_KEY=

# Notion Database ID (必需，如果要保存到 Notion)
# 数据库 ID 可以从 Notion 数据库 URL 中获取
# 例如: https://www.notion.so/username/DATABASE_ID?v=...
NOTION_DATABASE_ID=

# AI API Key (可选，用于生成营销文案, 定位用户群体)
# 支持 OpenAI 和 DeepSeek 等兼容 OpenAI API 的服务
# - OpenAI: https://platform.openai.com/api-keys
# - DeepSeek: https://platform.deepseek.com/api_keys
# 如果不配置，将使用默认生成的文案
OPENAI_API_KEY=

# AI API Base URL (可选)
# OpenAI: https://api.openai.com/v1 (默认)
# DeepSeek: https://api.deepseek.com
# 其他兼容服务: 根据服务商提供的地址
OPENAI_API_BASE=

# AI Model (可选，默认为 gpt-3.5-turbo)
# OpenAI: gpt-3.5-turbo, gpt-4, gpt-4-turbo-preview, gpt-4o 等
# DeepSeek: deepseek-chat, deepseek-coder 等
OPENAI_MODEL=gpt-3.5-turbo

